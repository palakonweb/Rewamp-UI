export const pillExpandNavbarPrompt = `Compact black pill bottom navigation bar with 5 icon-only tabs (Home, Category, Cart, Save, Profile) tightly packed inside a rounded-full dark container with minimal padding — no extra whitespace around the icons. On hover or when active, a tab smoothly expands from a small square icon button into a wider rounded pill revealing its label next to the icon, while every other tab stays collapsed to just its icon. The currently active tab (when not hovered) keeps a faint background tint so it stays identifiable. Use Framer Motion's shared \`layout\` prop on each tab button so the width change animates fluidly, and fade the label in with a short opacity transition (no separate width animation on the label itself, to avoid competing/jittery animations) — tuned to a slow, soft spring (moderate stiffness, high damping, slightly heavier mass) so the expand/collapse feels smooth rather than snappy. Built with React, Framer Motion, Tailwind CSS, and lucide-react icons.`;
