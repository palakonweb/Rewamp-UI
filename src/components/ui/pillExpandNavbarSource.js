export const pillExpandNavbarPrompt = `Compact black pill bottom navigation bar with 5 icon-only tabs (Home, Category, Cart, Save, Profile) tightly packed inside a rounded-full dark container with minimal padding — no extra whitespace around the icons. On hover or when active, a tab smoothly expands from a small fixed-width square icon button to a fixed wider pill width (same width for every tab, regardless of label length, so the overall navbar never shifts size) revealing its label next to the icon, while every other tab stays collapsed to just its icon. The currently active tab (when not hovered) keeps a faint background tint so it stays identifiable. Animate each tab's \`width\` directly with Framer Motion using a slow, soft spring (low stiffness, moderate-high damping, slightly heavier mass) so the expand/collapse feels gentle and deliberate rather than snappy, and fade the label in with a slightly delayed opacity transition (no separate width animation on the label itself, to avoid competing/jittery animations). Built with React, Framer Motion, Tailwind CSS, and lucide-react icons.`;
